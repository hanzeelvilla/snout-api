name: Scheduled Workflow

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
    schedule:
        runs-on: ubuntu-24.04
        steps:
            - name: Saludar
              run: echo "Hola desde Github Actions"
