services:
  db:
    image: postgres:17
    restart: always
    ports:
      - "${DB_PORT}:5432"
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    container_name: snout-db
    volumes:
      - ./postgres:/var/lib/postgresql/data